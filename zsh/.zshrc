# Nice prompt
PROMPT="%F{yellow}%1~ %F{blue}> %f "

# What's your editor?
EDITOR=vim

# ls with colors
alias ls='ls --color=always'

# Path
PATH=$PATH:$HOME/bin:$HOME/.local/bin

# Completion
autoload -U compinit && compinit
